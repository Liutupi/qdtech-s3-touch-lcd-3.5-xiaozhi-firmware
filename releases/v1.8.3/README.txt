QDTech S3 Touch LCD 3.5 — v1.8.3 OTA Release

Use qdtech-s3-touch-lcd-3.5-v1.8.3-app.bin for OTA updates.
Use qdtech-s3-touch-lcd-3.5-v1.8.3-full.bin only for serial recovery.

v1.8.3 is the OTA successor to v1.8.2. It contains the same validated
QDTech provisioning fix, normal startup sequence, automatic phone config
page, and Chinese app entry labels, with the version increment required for
devices already running v1.8.2 to discover the update.

Build verification completed. Final hardware verification is via remote OTA.
